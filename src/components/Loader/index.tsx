import { CircularProgress } from '@mui/material';

const Loader = () => {
  return (
    <CircularProgress className="absolute top-[50%] left-0 right-0 my-0 mx-auto" />
  );
};

export default Loader;
